services:
  - type: web
    name: infant-cry-analyzer
    env: python
    buildCommand: |
      pip install -r requirements.txt
    startCommand: gunicorn app:app
    runtime: python
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
    plan: free